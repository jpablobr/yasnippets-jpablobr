#name : context "..." do ... end
#key : cont
#group : rails.shoulda
#condition : (or (rails/unit-test?) (rails/functional-test?))
# --
context "${description}" do
  setup do
    ${setup}
  end
 sh$0
end
